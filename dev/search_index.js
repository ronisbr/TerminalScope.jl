var documenterSearchIndex = {"docs":
[{"category":"section","location":"man/type_inspector/#man_type_inspector","page":"Type Inspector","text":"The type-instability inspector shows a method's source code annotated with the inferred types — in the spirit of Cthulhu.@descend — and lets the user descend through its call sites. It can be entered in two ways:\n\nPressing i on any frame of a viewer whose row carries a method instance;\nDirectly, through the descend mode:\n\n@scope descend f(x, y)\n\nThe call is not executed; only its argument types are used to select the inspected method specialization. The function form takes the argument types explicitly:\n\nscope_descend(f, Tuple{Int, Float64})\n\nnote: Note\nThe inspector is backed by Cthulhu, which is loaded automatically on the first use (with a status notice, since it takes a moment). Loading Cthulhu invalidates compiled code of unrelated packages, which is why it is not loaded before it is needed.","title":"Type Inspector"},{"category":"section","location":"man/type_inspector/#The-Viewer","page":"Type Inspector","text":"(Image: Type inspector)\n\nThe top pane shows the annotated source of the inspected method: the inferred types are attached to the expressions whose type is not concrete, colored by stability using the theme colors (which adapt to the light variant):\n\nError color (coral): An abstract type — the classic type instability;\nWarning color (gold): A small Union, which Julia usually handles well;\nPrimary color (cyan): A concrete (stable) type.\n\nThe t key toggles the pane to the typed IR (the @code_warntype-like view), which is also used automatically when the source cannot be mapped (e.g. generated functions).\n\nThe bottom pane lists the call sites of the method with their inferred return types, colored by the same convention. Union-split call sites list each target as a sub-entry (↳), and dynamic dispatches are tagged with [dyn]. The source pane highlights and centers the line of the selected call site.\n\nEnter / → descends into the selected call site;\nBackspace / ← ascends back to the caller;\nq / Esc closes the inspector, returning to the view it was opened from.\n\nThe header shows the inspected method, its inferred return type, the descend depth, and how many call sites are type-unstable.","title":"The Viewer"},{"category":"section","location":"man/type_inspector/#Examples","page":"Type Inspector","text":"julia> using TerminalScope\n\njulia> unstable(x) = x > 0 ? 1 : 2.0;\n\njulia> caller(x) = unstable(x) + 1;\n\njulia> @scope descend caller(1)","title":"Examples"},{"category":"section","location":"man/invalidations/#Invalidations","page":"Invalidations","text":"The invalidations mode of @scope records the method invalidations caused by an expression — typically code loading — and opens the viewer with the invalidation forest:\n\n@scope invalidations expr\n\nThe typical usage wraps a package load:\n\njulia> @scope invalidations using SomePackage\n\nThe recording is performed by SnoopCompileCore.@snoop_invalidations, and the raw data is organized into trees by SnoopCompile.invalidation_trees.\n\nnote: Note\nSnoopCompile itself invalidates compiled code when loaded, so TerminalScope loads it on the first use of this feature — deliberately after the recording, so its own invalidations do not pollute the capture. If it cannot be loaded, the raw data is returned so nothing is lost.","title":"Invalidations"},{"category":"section","location":"man/invalidations/#The-Viewer","page":"Invalidations","text":"(Image: Invalidations viewer)\n\nThe first level lists the triggers — one row per event that invalidated compiled code — sorted by the number of invalidated method instances:\n\ninserting f(::T): A new, more specific method was defined;\ndeleting f(::T): A method was removed;\nrebinding Main.x: A binding (e.g. a constant) was redefined;\nunattributed invalidations: Invalidations that SnoopCompile could not attribute to a recorded definition, common when package images are involved.\n\nDescending into a trigger shows the invalidated method specializations and, recursively, the callers invalidated through them. Method-table backedges are listed under an intermediate row named after the invalidated call signature. The cost column shows the number of invalidated instances in each subtree.\n\nPressing i on an invalidated instance opens the Type Inspector on it, so the loose type that made the code vulnerable to invalidation can be found immediately.","title":"The Viewer"},{"category":"section","location":"man/invalidations/#Viewing-Collected-Data","page":"Invalidations","text":"scope_invalidations(invs)\n\naccepts either the raw data returned by SnoopCompileCore.@snoop_invalidations or the trees returned by SnoopCompile.invalidation_trees.","title":"Viewing Collected Data"},{"category":"section","location":"man/invalidations/#Examples","page":"Invalidations","text":"julia> using TerminalScope\n\njulia> @scope invalidations using CSV\n\njulia> invs = SnoopCompileCore.@snoop_invalidations using DataFrames;\n\njulia> scope_invalidations(invs)","title":"Examples"},{"category":"section","location":"lib/library/#Library","page":"Library","text":"Documentation for TerminalScope.jl.","title":"Library"},{"category":"macro","location":"lib/library/#TerminalScope.@scope","page":"Library","text":"@scope([mode,] [options...,] expr)\n\nAnalyze expr and open the interactive terminal viewer with the result. The first argument selects the analysis; without it, expr runs under the sampling profiler.\n\nModes\n\n@scope [delay = seconds] [n = samples] expr: Runtime profile. delay is the time   between two samples and n the sample buffer size, forwarded to Profile.init   (they persist for the session). The compilation time of the run is shown in the   header.\n@scope allocs [sample_rate = 1.0] [warmup = true] expr: Allocation profile, ranked   by bytes with the u key re-ranking by allocation counts. expr runs once as a   warm-up so compiler allocations stay out of the profile — pass warmup = false when   its side effects must happen only once, and consider a lower sample_rate if the   profiled run may still compile fresh code.\n@scope inference expr: Type-inference profile via   SnoopCompileCore.@snoop_inference. Run it on code that has not been compiled yet   (typically in a fresh session).\n@scope invalidations expr: Method invalidations caused by expr, typically   @scope invalidations using SomePackage.\n@scope descend f(x, y): Type-instability inspector on the call, like   Cthulhu.@descend. The call is not executed; only its argument types are used.\n\nThe value of expr is discarded (except by descend, which never runs it). The corresponding function forms — scope_profile, scope_allocs, scope_inference, scope_invalidations, and scope_descend — open the viewer on already-collected data.\n\n\n\n\n\n","title":"TerminalScope.@scope"},{"category":"function","location":"lib/library/#TerminalScope.scope_profile","page":"Library","text":"scope_profile(g; kwargs...) -> Nothing\n\nOpen the interactive terminal viewer for the flame graph rooted at g, a node returned by FlameGraphs.flamegraph. The viewer takes over the terminal until the user quits with the q key.\n\nKeywords\n\ncompile::Union{CompileStats, Nothing}: Compilation measurements of the profiled   expression, shown in the header when available.   (Default: nothing)\ntheme::Symbol: Theme variant, either :dark or :light. See also theme!.   (Default: DEFAULT_THEME[])\n\n\n\n\n\nscope_profile(; kwargs...) -> Nothing\n\nBuild the flame graph from the current Profile data and open the interactive terminal viewer. Print a warning and return without opening the viewer when no profile data is available.\n\nKeywords\n\ncompile::Union{CompileStats, Nothing}: Compilation measurements of the profiled   expression, shown in the header when available.   (Default: nothing)\ntheme::Symbol: Theme variant, either :dark or :light. See also theme!.   (Default: DEFAULT_THEME[])\n\n\n\n\n\n","title":"TerminalScope.scope_profile"},{"category":"function","location":"lib/library/#TerminalScope.scope_inference","page":"Library","text":"scope_inference(tinf; theme::Symbol = DEFAULT_THEME[]) -> Nothing\n\nOpen the interactive terminal viewer for the inference timing tree tinf returned by SnoopCompileCore.@snoop_inference, with the frame costs shown as inference plus LLVM compilation times. The viewer takes over the terminal until the user quits with the q key. theme selects the :dark or :light variant; see also theme!.\n\n\n\n\n\n","title":"TerminalScope.scope_inference"},{"category":"function","location":"lib/library/#TerminalScope.scope_allocs","page":"Library","text":"scope_allocs(results = Profile.Allocs.fetch(); theme::Symbol = DEFAULT_THEME[]) -> Nothing\n\nOpen the interactive terminal viewer for the allocation profile results returned by Profile.Allocs.fetch(). The tree shows where memory was allocated, ranked by allocated bytes; the u key re-ranks it by number of allocations. Descending past the deepest frame lists the allocated types. When the profile was collected with a sample rate below 1.0, the shown bytes and counts are the recorded fraction, not the totals. The viewer takes over the terminal until the user quits with the q key. theme selects the :dark or :light variant; see also theme!.\n\nSee also @scope.\n\n\n\n\n\n","title":"TerminalScope.scope_allocs"},{"category":"function","location":"lib/library/#TerminalScope.scope_invalidations","page":"Library","text":"scope_invalidations(invs) -> Nothing\n\nOpen the interactive terminal viewer for the method invalidations invs, either the raw data returned by SnoopCompileCore.@snoop_invalidations or the trees returned by SnoopCompile.invalidation_trees. The first level lists the methods whose definition triggered invalidations; descending shows the invalidated specializations and, recursively, the callers invalidated through them. The i key opens the type-instability inspector on the selected instance. The viewer takes over the terminal until the user quits with the q key. theme selects the :dark or :light variant; see also theme!.\n\nOrganizing raw invalidation data requires SnoopCompile, which is loaded on the first use (its load-time invalidations do not affect sessions that only use the profilers). When it cannot be loaded, a warning is printed and the raw data is returned so it can be viewed later.\n\nSee also @scope.\n\n\n\n\n\n","title":"TerminalScope.scope_invalidations"},{"category":"function","location":"lib/library/#TerminalScope.scope_descend","page":"Library","text":"scope_descend(f, types::Type{<:Tuple} = Tuple{}; theme::Symbol = DEFAULT_THEME[]) -> Nothing\n\nOpen the interactive type-instability inspector on the method of f specialized for the argument types types, similar to Cthulhu.descend. The inspector shows the source code annotated with the inferred types, colored by type stability, and the call sites of the method, which can be descended into with the Enter key. The viewer takes over the terminal until the user quits with the q key. theme selects the :dark or :light variant; see also theme!.\n\nThe inspector requires Cthulhu, which is loaded on the first use (its load-time invalidations do not affect sessions that only use the profilers).\n\nSee also @scope.\n\n\n\n\n\n","title":"TerminalScope.scope_descend"},{"category":"section","location":"man/allocation_profile/#Allocation-Profile","page":"Allocation Profile","text":"The allocs mode of @scope runs an expression under the allocation profiler (Profile.Allocs) and opens the viewer with the tree of allocation sites:\n\n@scope allocs [sample_rate = 1.0] [warmup = true] expr\n\nThe expression is first executed once as a warm-up, so its compilation happens before the recording starts, and then executed again under the profiler. The value of expr is discarded.\n\nThe following options are available:\n\nsample_rate: Fraction of the allocations recorded by the profiler. With the default 1.0, every allocation is recorded and the shown bytes and counts are exact; with a smaller value, they are the recorded fraction. (Default: 1.0)\nwarmup: If true, run expr once before profiling it. (Default: true)\n\nwarning: Warning\nThe warm-up run means expr is executed twice. Pass warmup = false when its side effects must happen only once — and, in that case, consider a lower sample_rate: profiling a first call at full rate records a stack trace for every allocation of the compiler itself, which can take many minutes and look like a hang.","title":"Allocation Profile"},{"category":"section","location":"man/allocation_profile/#The-Viewer","page":"Allocation Profile","text":"(Image: Allocation profile viewer)\n\nThe tree is ranked by allocated bytes and the header shows the totals of both units. Pressing u re-ranks the whole tree by allocation counts (and back), recomputing every percentage and cost bar, so both the \"few huge buffers\" and the \"many small allocations\" problems are easy to spot.\n\nDescending past the deepest frame of a branch lists the allocated types (Vector{Float64}, String, ...) as leaves with their own costs, so the viewer shows not only where the allocation happened but also what was allocated.\n\nTo keep the tree readable:\n\nCompiler-mangled names are demangled (keyword bodies like #f#123 become f, and anonymous functions are shown as λ#37);\nPure keyword wrapper frames (kwcall and same-named keyword bodies) are collapsed;\nThe C runtime allocator frames at the leaf side of the stacks are stripped, so branches bottom out at the allocating Julia frame.","title":"The Viewer"},{"category":"section","location":"man/allocation_profile/#Per-Line-Costs-in-the-Source-Panel","page":"Allocation Profile","text":"In the allocation viewer, the source panel gains a heat-colored column between the line numbers and the code with the cost charged to each line, following the active unit. Within a file, each allocation is charged only to the innermost frame of that file, so caller lines stay clean while user files are still credited for allocations that bottom out inside Base.","title":"Per-Line Costs in the Source Panel"},{"category":"section","location":"man/allocation_profile/#Viewing-Collected-Data","page":"Allocation Profile","text":"scope_allocs()         # Uses `Profile.Allocs.fetch()`.\nscope_allocs(results)  # Uses an `AllocResults` collected before.","title":"Viewing Collected Data"},{"category":"section","location":"man/allocation_profile/#Examples","page":"Allocation Profile","text":"julia> using TerminalScope\n\njulia> @scope allocs my_workload()\n\njulia> @scope allocs sample_rate = 0.01 cold_workload()\n\njulia> @scope allocs warmup = false push_to_global_state!()","title":"Examples"},{"category":"section","location":"man/inference_profile/#Inference-Profile","page":"Inference Profile","text":"The inference mode of @scope profiles Julia's type inference while an expression runs, using SnoopCompileCore.@snoop_inference, and opens the viewer with the inference timing tree:\n\n@scope inference expr\n\nThe frame costs are shown as inference plus LLVM compilation times, and the tree can be navigated exactly like a runtime profile. The value of expr is discarded.\n\n(Image: Inference profile viewer)\n\nThe times scale automatically from minutes down to microseconds and nanoseconds, so fast inference frames keep a meaningful cost instead of rounding to zero.\n\nnote: Note\nTo capture the inference cost of the first call, expr must contain code that has not been compiled yet. Run this mode in a fresh session for representative results.","title":"Inference Profile"},{"category":"section","location":"man/inference_profile/#Viewing-Collected-Data","page":"Inference Profile","text":"scope_inference(tinf)\n\nopens the viewer for the timing tree tinf returned by SnoopCompileCore.@snoop_inference.","title":"Viewing Collected Data"},{"category":"section","location":"man/inference_profile/#Examples","page":"Inference Profile","text":"julia> using TerminalScope\n\njulia> @scope inference my_uncompiled_pipeline()\n\njulia> tinf = SnoopCompileCore.@snoop_inference first_call();\n\njulia> scope_inference(tinf)","title":"Examples"},{"category":"section","location":"man/navigation/#man_navigation","page":"Navigation and Themes","text":"","title":"Navigation and Themes"},{"category":"section","location":"man/navigation/#The-Main-View","page":"Navigation and Themes","text":"Every profile viewer shares the same layout: the frame list on the left and the source panel on the right, updated live while navigating.\n\n[1] Frames: One level of the tree at a time — the current node as a pinned parent row (⬑) followed by its children, sorted by cost. Entering a node auto-descends through single-child chains until the next branching point, and going back inverts the auto-descent in one key press.\n[2] Source: A compact information strip (name, tags, costs, and method signature) above the syntax-highlighted source of the selected row, with the frame line marked with ▶.\n\nThe panels are numbered like in LazyGit: the number keys jump straight to a panel, and the movement keys act on the focused one.\n\nKeys Action\n↑ / ↓ Move the cursor (list) or scroll the code (source panel)\nj k h l g G Vim-style aliases of ↓ ↑ ← → Home End\nPgUp / PgDn Move or scroll one page\nHome / End Go to the first / last row, or the top / bottom of the code\nEnter / → Enter the node; on a leaf, focus the source panel\nBksp / ← Go back to the parent node\nTab, 1 / 2 Switch or jump the panel focus\n+ / - Maximize / restore the focused panel\n0 / $ Source panel: go to the line start / end\n^D / ^U Source panel: scroll half a page\n/ Search frames in the whole tree (case-insensitive)\nn / N Jump to the next / previous search match\ni Inspect the selected frame for type instabilities\nu Allocations: toggle between bytes and allocation counts\n? Toggle the help dialog\nq Quit\n\nThe status bar always shows the most important bindings of the active view, and the ? dialog lists all of them:\n\n(Image: Help dialog)","title":"The Main View"},{"category":"section","location":"man/navigation/#Mouse","page":"Navigation and Themes","text":"The mouse is supported as well: the wheel scrolls the panel under the cursor — moving the selection in the lists and the view in the code panes — and a left click focuses the clicked panel, moving the selection to the clicked row. Clicking the selected row again enters it, and a click closes the help dialog.","title":"Mouse"},{"category":"section","location":"man/navigation/#Frame-Search","page":"Navigation and Themes","text":"Pressing / opens a search prompt in the status bar, styled after the Neovim command line:\n\n(Image: Search prompt)\n\nConfirming with Enter collects every frame of the whole profile tree whose name or source location contains the query (case-insensitively) and navigates the viewer to the first match — descending or ascending levels as needed, with the cursor placed on the matching row. n and N then cycle through the matches, wrapping around, while the status bar reports the current position:\n\n(Image: Search match)\n\nEsc cancels the prompt without searching, and Backspace edits the query while it is open. Since the search covers the entire tree, it is the fastest way to answer \"where is my function in this profile\" without descending manually.","title":"Frame Search"},{"category":"section","location":"man/navigation/#Themes","page":"Navigation and Themes","text":"The viewers ship with a dark and a light theme, matching the SatelliteAnalysis.jl presentation palette: navy structure, amber chrome, cyan data emphasis, and magenta secondary emphasis.\n\nThe default variant is :dark. It can be changed for the session:\n\nTerminalScope.theme!(:light)\n\nor per invocation, since every function entry point accepts a theme keyword:\n\nscope_profile(g; theme = :light)\n\n(Image: Light theme)","title":"Themes"},{"category":"section","location":"man/navigation/#Customizing-the-Colors","page":"Navigation and Themes","text":"Every color of both variants can be overridden through Preferences.jl:\n\n# Colors accept an xterm-256 code (0-255) or a hex string (quantized to xterm-256).\nTerminalScope.set_theme_color!(:dark, :accent, \"#38BDF8\")\nTerminalScope.set_theme_color!(:light, :bg, 255)\n\n# The :selection slot is the background of the row under the cursor.\nTerminalScope.set_theme_color!(:dark, :selection, 236)\n\n# Restore the default palette of both variants.\nTerminalScope.reset_theme_colors!()\n\nThe available slots are :bg, :border, :border_focus, :text, :text_dim, :text_bright, :primary, :secondary, :accent, :success, :warning, :error, :title, and :selection. The overrides are stored in the LocalPreferences.toml file of the active project and take effect after restarting Julia.","title":"Customizing the Colors"},{"category":"section","location":"man/navigation/#Reference","page":"Navigation and Themes","text":"","title":"Reference"},{"category":"function","location":"man/navigation/#TerminalScope.theme!","page":"Navigation and Themes","text":"theme!(variant::Symbol) -> Symbol\n\nSet the default theme variant of the TerminalScope viewers to variant, either :dark or :light, and return it. Every entry point also accepts a theme keyword overriding this default for one invocation.\n\n\n\n\n\n","title":"TerminalScope.theme!"},{"category":"function","location":"man/navigation/#TerminalScope.set_theme_color!","page":"Navigation and Themes","text":"set_theme_color!(\n    variant::Symbol,\n    slot::Symbol,\n    color::Union{Integer, AbstractString}\n) -> Nothing\n\nPersist color as the color of the theme slot of variant using Preferences.jl, overriding the default of THEME_DEFAULTS. The change is written to the LocalPreferences.toml file of the active project and takes effect after restarting Julia. The function throws when the variant, slot, or color is invalid.\n\nArguments\n\nvariant::Symbol: Theme variant, either :dark or :light.\nslot::Symbol: Color slot, one of THEME_SLOTS — :bg, :border,   :border_focus, :text, :text_dim, :text_bright, :primary, :secondary,   :accent, :success, :warning, :error, or :title — or :selection, the   background of the row under the cursor.\ncolor::Union{Integer, AbstractString}: Color as an xterm-256 code (0-255) or a hex   string like \"#F59E0B\", which is quantized to the closest xterm-256 color.\n\nExtended help\n\nThrows\n\nArgumentError: The variant is not :dark or :light, the slot is not one of   THEME_SLOTS, or the color is neither a code in 0-255 nor a \"#RRGGBB\"   string.\n\nExamples\n\njulia> TerminalScope.set_theme_color!(:dark, :accent, \"#38BDF8\")\n\njulia> TerminalScope.set_theme_color!(:light, :bg, 255)\n\n\n\n\n\n","title":"TerminalScope.set_theme_color!"},{"category":"function","location":"man/navigation/#TerminalScope.reset_theme_colors!","page":"Navigation and Themes","text":"reset_theme_colors!() -> Nothing\n\nDelete every theme color preference of both variants, restoring the default colors of THEME_DEFAULTS after restarting Julia.\n\n\n\n\n\n","title":"TerminalScope.reset_theme_colors!"},{"category":"constant","location":"man/navigation/#TerminalScope.THEME_SLOTS","page":"Navigation and Themes","text":"THEME_SLOTS\n\nColor slots of a TerminalScope theme, in the order of the Tachikoma.Theme constructor. Each slot of each variant can be overridden with a Preferences.jl preference named \"<variant>_<slot>\", e.g. \"dark_bg\" (see set_theme_color!).\n\n\n\n\n\n","title":"TerminalScope.THEME_SLOTS"},{"category":"constant","location":"man/navigation/#TerminalScope.THEME_DEFAULTS","page":"Navigation and Themes","text":"THEME_DEFAULTS\n\nDefault xterm-256 color codes of the THEME_SLOTS of the :dark and :light theme variants, matching the SatelliteAnalysis.jl presentation palette: navy structure, amber chrome (titles, costs, focus), cyan data emphasis, and magenta secondary emphasis, quantized to the closest xterm-256 colors. The light variant uses a white surface with the accents darkened for contrast as in the original.\n\n\n\n\n\n","title":"TerminalScope.THEME_DEFAULTS"},{"category":"constant","location":"man/navigation/#TerminalScope.SELECTION_DEFAULTS","page":"Navigation and Themes","text":"SELECTION_DEFAULTS\n\nDefault xterm-256 color codes of the :selection slot of the :dark and :light theme variants: the background used to highlight the row under the cursor. The slot lives outside Tachikoma.Theme, which has no selection color, but is overridden through the same preference mechanism as the THEME_SLOTS (see set_theme_color!).\n\n\n\n\n\n","title":"TerminalScope.SELECTION_DEFAULTS"},{"category":"section","location":"man/runtime_profile/#Runtime-Profile","page":"Runtime Profile","text":"The default mode of @scope runs an expression under Julia's sampling profiler and opens the interactive viewer with the resulting call tree:\n\n@scope([delay = seconds,] [n = samples,] expr)\n\nThe value of expr is discarded. While the expression runs, its compilation time is also measured and shown in the viewer header.\n\nThe following options are available:\n\ndelay: Time [s] between two profiler samples, forwarded to Profile.init. Use a smaller delay to resolve short-running code. (Default: the current Profile.init setting, usually 0.001)\nn: Size of the sample buffer, forwarded to Profile.init. A smaller delay usually requires a larger buffer. (Default: the current Profile.init setting)\n\nnote: Note\nExactly as with Profile.init, the delay and n settings persist for the rest of the session.","title":"Runtime Profile"},{"category":"section","location":"man/runtime_profile/#The-Viewer","page":"Runtime Profile","text":"(Image: Runtime profile viewer)\n\nThe header shows the total number of samples, the sampling delay, the estimated wall clock time, and — when the expression was run through the macro — the time spent compiling, with the fraction of the run and the fraction due to recompilation.\n\nThe frame list shows one level of the call tree at a time: the current node as a pinned parent row (marked with ⬑) followed by its children, sorted by cost. Each row shows the frame name, its tags, the source location, and the right-aligned cost columns: a mini cost bar, the inclusive sample count, and the percentage of the total cost. The viewer starts past the REPL and script evaluation machinery, at the first level where the tree branches.\n\nThe rows are tagged according to the frame kind:\n\n[dyn]: The call was flagged as a runtime (dynamic) dispatch;\n[GC]: The frame is a garbage collection event;\n[inf]: The frame belongs to the Julia compiler (type inference).\n\nThe header additionally aggregates the samples spent in type inference, so compilation inside the profiled expression is visible at a glance.","title":"The Viewer"},{"category":"section","location":"man/runtime_profile/#Viewing-Collected-Data","page":"Runtime Profile","text":"The function form opens the viewer on data collected by Profile.@profile (or on a flame graph already built with FlameGraphs.flamegraph):\n\nscope_profile()   # Uses the current `Profile` data.\nscope_profile(g)  # Uses the flame graph `g`.","title":"Viewing Collected Data"},{"category":"section","location":"man/runtime_profile/#Examples","page":"Runtime Profile","text":"julia> using TerminalScope\n\njulia> @scope sum(rand(1000, 1000) for _ in 1:100)\n\njulia> @scope delay = 0.0001 my_fast_function()\n\njulia> Profile.@profile workload(); scope_profile()","title":"Examples"},{"category":"section","location":"#TerminalScope.jl","page":"Home","text":"This package provides an interactive terminal user interface (TUI) to diagnose the performance of Julia code. It bundles four analyses under one macro and one consistent drill-down interface:\n\nRuntime profile: where the time is spent, from the sampling profiler;\nAllocation profile: where the memory is allocated, including what was allocated;\nInference profile: where the compiler spends its time inferring types;\nInvalidations: which method definitions invalidate previously compiled code.\n\nOn top of those, a type-instability inspector — in the spirit of Cthulhu.@descend — shows the source code annotated with the inferred types and lets the user descend through the call sites, directly from any profiled frame.\n\n(Image: Runtime profile viewer)\n\nThe viewers support the mouse (the wheel scrolls the hovered panel and a click selects a row), vim-style navigation keys, and whole-tree frame search with the / key.","title":"TerminalScope.jl"},{"category":"section","location":"#Installation","page":"Home","text":"This package can be installed using:\n\njulia> using Pkg\njulia> Pkg.add(\"TerminalScope\")","title":"Installation"},{"category":"section","location":"#Quick-Start","page":"Home","text":"All the analyses are reached through the macro @scope, whose first argument selects the mode:\n\nusing TerminalScope\n\n@scope f(x)                          # Runtime profile (default mode).\n@scope allocs f(x)                   # Allocation profile.\n@scope inference f(x)                # Type-inference profile.\n@scope invalidations using SomePkg   # Method invalidations.\n@scope descend f(x)                  # Type-instability inspector.\n\nEach mode opens a full-screen viewer that takes over the terminal until the user quits with the q key. The main view shows the frame list on the left and the source code of the selected row — updated live while navigating — on the right. See Navigation and Themes for the complete set of key bindings.\n\nThe corresponding function forms open the viewer on already-collected data: scope_profile, scope_allocs, scope_inference, scope_invalidations, and scope_descend.\n\nnote: Note\nThe type inspector and the invalidation analysis are backed by Cthulhu.jl and SnoopCompile.jl. Loading those packages invalidates compiled code of unrelated packages, so TerminalScope only loads them on the first use of the features that need them. Sessions that only use the profilers never pay that cost.","title":"Quick Start"},{"category":"section","location":"#Manual-Outline","page":"Home","text":"Pages = [\n    \"man/runtime_profile.md\",\n    \"man/allocation_profile.md\",\n    \"man/inference_profile.md\",\n    \"man/invalidations.md\",\n    \"man/type_inspector.md\",\n    \"man/navigation.md\",\n]\nDepth = 2","title":"Manual Outline"}]
}
